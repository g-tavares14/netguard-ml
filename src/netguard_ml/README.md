# Código Python do NetGuard ML

Pacote do pipeline. O dataset oficial é o CICIoT2023 subsample em `data/raw/ciciot2023-neto-subsample/` ([docs/dataset.md](../../docs/dataset.md)).

Hoje existe a leitura dos splits e a inspeção do schema das features (`netguard_ml.data`). Ainda não há preprocessing de modelo, treino nem contrato de inferência.

Como executar: [docs/como-executar.md](../../docs/como-executar.md).
